<h1>Estos son mi perros: </h1>

<ul>
  <%@dogs.each do |dog| %>
    <li>
      <%= link_to dog.name, dog_path(dog) %>
    </li>
    <li> <%= dog.name %> y su raza es: <%= dog.breed %> </li>
  <% end %>
</ul>
